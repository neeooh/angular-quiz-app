// Immediately Invoking Function Expression (IIFE)
// This function runs automatically when the page loads
(function(){
    angular.module("turtleFacts", []); //creates new Angular module
    //angular.module("turtleFacts"); //this goes and finds/fetches an already existing module
    
})();